{% extends 'base.html.twig' %}

{% block title %}SondageAnswer{% endblock %}

{% block body %}
    <h1>SondageAnswer</h1>

    <table class="table-show">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sondage_answer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sondage_answer_index') }}">back to list</a>

    <a href="{{ path('sondage_answer_edit', {'id': sondage_answer.id}) }}">edit</a>

    {{ include('sondage_answer/_delete_form.html.twig') }}
{% endblock %}
