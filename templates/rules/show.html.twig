{% extends 'base.html.twig' %}

{% block title %}Rules{% endblock %}

{% block body %}
    <h1>Rules</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rule.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ rule.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rules_index') }}">back to list</a>

    <a href="{{ path('rules_edit', {'id': rule.id}) }}">edit</a>

    {{ include('rules/_delete_form.html.twig') }}
{% endblock %}
