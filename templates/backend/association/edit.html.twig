{% extends 'base.html.twig' %}

{% block title %}Edit Association{% endblock %}

{% block body %}
    <h1>Edit Association</h1>

    {{ form_start(form) }}
    {% form_theme form 'partials/template_form.html.twig' %}
    {{ form_widget(form) }}
    <button class="btn btn-success m-4">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    <div class="row">
        <a href="{{ path('admin_association_index') }}" class="btn btn-info m-4">back to list</a>
        {{ include('backend/association/_delete_form.html.twig') }}
    </div>

{% endblock %}
