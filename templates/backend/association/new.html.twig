{% extends 'base.html.twig' %}

{% block title %}New Association{% endblock %}

{% block body %}
    <h1>Create new Association</h1>

    {{ form_start(form) }}
    {% form_theme form 'partials/template_form.html.twig' %}
    {{ form_widget(form) }}
    <button class="btn btn-success m-4">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('association_index') }}" class="btn btn-info m-4">back to list</a>
{% endblock %}
