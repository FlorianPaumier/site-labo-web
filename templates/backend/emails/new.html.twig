{% extends 'base.html.twig' %}

{% block title %}New Emails{% endblock %}

{% block body %}
    <h1>Create new Emails</h1>

    <section class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">New</button>
        {{ form_end(form) }}
    </section>


    <a href="{{ path('admin_emails_index') }}">back to list</a>
{% endblock %}
