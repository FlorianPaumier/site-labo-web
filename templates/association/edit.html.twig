{% extends 'base.html.twig' %}

{% block title %}Edit Association{% endblock %}

{% block body %}
    <h1>Edit Association</h1>

    {{ include('partials/_form.html.twig', {'button_label': 'Update', 'class' : 'association'}) }}
    <div class="row">
        <a href="{{ path('association_index') }}" class="btn btn-info m-4">back to list</a>
        {{ include('association/_delete_form.html.twig') }}
    </div>

{% endblock %}
